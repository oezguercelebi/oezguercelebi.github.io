# oezguercelebi.github.io

Personal, evidence-first portfolio for Özgür Celebi.

## Local preview

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173`.

The site is intentionally plain HTML and CSS so GitHub Pages can serve it directly without a build step.
